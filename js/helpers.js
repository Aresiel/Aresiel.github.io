function red(text){
    return `<span style="color: var(--red)">${text}</span>`
}

function green(text){
    return `<span style="color: var(--green)">${text}</span>`
}

function blue(text){
    return `<span style="color: var(--blue)">${text}</span>`
}